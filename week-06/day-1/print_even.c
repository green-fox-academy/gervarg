#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Create a program that prints all the even numbers between 0 and 500
    int i = 0;
    int numbers = 500;
    for (i = 0; i <= numbers; i++) {
            printf("%d\n", i);
    }

    return 0;
}
